export type StakeData = {
  totalStaked: number;
  yourStaked: number;
  apr: number;
  depositFee: number;
};

export type FarmDataType = {
  name: string;
  liq: number;
  apr: number;
  stake: number;
};
